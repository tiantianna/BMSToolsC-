<mui:ModernWindow x:Class="bms.startup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:input="clr-namespace:System.Windows.Input;assembly=PresentationCore"
        WindowStartupLocation="CenterScreen"
        Icon="GXlogo2.ico"
        WindowState="Maximized"
        Style="{StaticResource BlankWindow}"
        Title="HC221.0.0.20200316R"
        LogoData="F1M59,1C82.109,0.592 131.703,43.981 125,71 121.558,84.874 77.578,126.525 55,122 54.667,121.333 54.333,120.667 54,120 58.921,103.848 97.031,74.05 100,61 99,61 98,61 97,61 90.132,65.02 44.702,84 43,88 43.667,88 44.333,88 45,88 53.175,90.684 57.496,83.374 66,85 66,86.333 66,87.667 66,89 59.699,96.661 54.079,104.134 46,110 44.667,110 43.333,110 42,110 36.632,106.36 33.25,101.709 29,97 28.366,72.076 53.191,15.953 81,28 86.175,30.045 88.245,32.432 89,39 84.447,43.534 81.921,43.13 75,46 76.632,48.557 76.232,48.424 79,50 82.333,50 85.667,50 89,50 91.666,47.667 94.334,45.333 97,43 97.635,28.663 93.71,21.758 84,18 50.145,8.821 26.208,37.04 14,54 9.973,59.595 9.672,65.9 3,69 3,65.334 3,61.666 3,58 11.535,35.345 38.89,12.686 59,1z" 
        Height="480" Width="720">
    <Window.Resources>

        <Style TargetType="{x:Type Label}">
            <Setter Property="Width" Value="450"/>
            <!--<Setter Property="Width" Value="150"/>-->
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"></Setter>
        </Style>
        <Style TargetType="{x:Type TextBox}" x:Key="data">
            <Setter Property="Width" Value="60"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style x:Key="receiveData" TargetType="{x:Type TextBox}" BasedOn="{StaticResource data}">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="179*"></RowDefinition>
            <RowDefinition Height="240*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5,5,5.333,5.333">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource sbsyh}" VerticalAlignment="Center" Style="{StaticResource MyStyle}" Width="160"></TextBlock>
                    <ComboBox Width="100" ItemsSource="{Binding CanIndexList}" SelectedIndex="{Binding SelectDeviceIndex}"/>
                    <TextBlock  Text="{DynamicResource cantongdao}" VerticalAlignment="Center" Margin="20,0,0,0" Style="{StaticResource MyStyle}" Width="100"></TextBlock>
                    <ComboBox Width="100" ItemsSource="{Binding CanChannelList}" SelectedIndex="{Binding SelectCanChannelIndex}"/>
                    <TextBlock Text="{DynamicResource baudrate}" Margin="20,0,0,0" VerticalAlignment="Center"  Style="{StaticResource MyStyle}" Width="85"></TextBlock>
                    <ComboBox Width="100" ItemsSource="{Binding CanBaudRateList}" SelectedIndex="{Binding SelectRate}"  DisplayMemberPath="Baudratename"/>
                    <Button x:Name="CanConnectBtn"  Content="{DynamicResource connect}" Margin="20,0,0,0" Width="85" Command="{Binding ConnectCanCommand}"  CommandParameter="{Binding ElementName=CanConnectBtn}" Style="{DynamicResource ButtonStyle1}" />
                    <Button Content="{DynamicResource zhuxiao}" Margin="10,0,0,0" Width="60" Command="{Binding LogoutCommand}" Style="{DynamicResource ButtonStyle1}" />
                    <ComboBox x:Name="cbLang" Margin="10,0,0,0"  Width="120" ItemsSource="{Binding CategoryI18nList}" DisplayMemberPath="Name" SelectedIndex="{Binding SelectI18n}" >
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding CbI18nClickCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ComboBox>
                    <Button x:Name="m_HelpButton" IsEnabled="True" Width="23" Height="23" Command="{Binding UserCommand}" Margin="15,0,0,0" ToolTip="用户管理" Visibility="{Binding ButtonVisible}">
                        <Button.Template>
                            <ControlTemplate>
                                <Grid>
                                    <Ellipse>
                                        <Ellipse.Stroke>
                                            <SolidColorBrush x:Name="m_Stroke" Color="Silver" />
                                        </Ellipse.Stroke>
                                        <Ellipse.Fill>
                                            <SolidColorBrush x:Name="m_Back" Color="White" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <Image Margin="2" Source="Images/user.png" />
                                </Grid>

                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="5,9.667,5.333,-0.334" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <!--<RowDefinition Height="auto" MaxHeight="300" MinHeight="100"></RowDefinition>-->
            </Grid.RowDefinitions>
            <TabControl SelectedIndex="{Binding Index}"   >
                <!--<TabControl SelectedIndex="1">-->
                <TabItem Header="{DynamicResource initconfig}" Style="{DynamicResource ModelTabItemStyle}"  Visibility="{Binding Itemlist[0]}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="159*"/>
                            <ColumnDefinition Width="533*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Orientation="Horizontal">
                                <Label Content="{DynamicResource bmubefore}" Width="180" VerticalAlignment="Center" Margin="3"  Style="{StaticResource label_gy}" VerticalContentAlignment="Center"/>
                                <TextBox Style="{StaticResource data}" Text="{Binding SlaveNumTarget}" IsEnabled="False" VerticalContentAlignment="Center"/>
                                <!--</StackPanel>
                            <StackPanel  Orientation="Horizontal" HorizontalAlignment="left">-->
                                <Label Content="{DynamicResource allselect}" FontSize="12" Margin="15,0,0,0" HorizontalContentAlignment="Left" Width="70" VerticalContentAlignment="Center"/>
                                <CheckBox Margin="3,0,0,0" VerticalContentAlignment="Center">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding All_Check_Factory}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="Unchecked">
                                            <i:InvokeCommandAction Command="{Binding All_UnCheck_Factory}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                                <Button Content="{DynamicResource getbmunum}" Background="#5bc0de" Margin="0,10,5,0" Width="170" Command="{Binding GetID}"/>
                                <Button x:Name="sendFacConfig" Content="{DynamicResource send}" Background="#5bc0de" Margin="0,10,5,0" Command="{Binding InitConfigCommand}"/>
                                <Button  Content="{DynamicResource readconfig}"  Background="#5bc0de"  Margin="0,10,5,0" Command="{Binding ReadFactoryConfigCommand}"/>
                                <Button  Content="{DynamicResource saveconfig}" Background="#5bc0de"  Margin="0,10,5,0" Command="{Binding SaveFactoryConfigCommand}"/>
                            </StackPanel>

                            <Border BorderBrush="#5bc0de" BorderThickness="1" Margin="0,5,0,5" Opacity=".6"></Border>
                            <!--<StackPanel Orientation="Horizontal" Margin="0,10,0,0">

                                <Label Content="从机编号:" VerticalAlignment="Center" Margin="3"  Style="{StaticResource label_gy}"/>
                                <TextBox Style="{StaticResource data}" Text="{Binding SlaveNum}"/>
                                <TextBox Style="{StaticResource receiveData}" Text="{Binding SlaveNum_rec}"/>
                                <Label Content="均衡模式:" Width="85" VerticalAlignment="Center" Margin="3"  Style="{StaticResource label_gy}" />
                                <ComboBox FontSize="10" Width="60" Margin="3" VerticalContentAlignment="Center" ItemsSource="{Binding BalanceModeList}" SelectedIndex="{Binding SelectBalanceMode}"/>
                               
                                <TextBox Style="{StaticResource receiveData}" Text="{Binding CellBalanMode_rec}"/>
                                <Label Content="从机监控单体总数:" VerticalAlignment="Center" Margin="3"  Style="{StaticResource label_gy}"/>
                                <TextBox Style="{StaticResource data}" Text="{Binding ChildModuleMonCellNumber}" />
                                <TextBox Style="{StaticResource receiveData}" Text="{Binding ChildModuleMonCellNumber_rec}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Label Content="从机监控温感总数:" VerticalAlignment="Center" Margin="3" Style="{StaticResource label_gy}" />
                                <TextBox Style="{StaticResource data}" Text="{Binding ChildMonModuleTemperatureNumber}"/>
                                <TextBox Style="{StaticResource receiveData}" Text="{Binding ChildMonModuleTemperatureNumber_rec}"/>
                                <Label Content="子模块A监控电池数目:" VerticalAlignment="Center" Margin="3"  Style="{StaticResource label_gy}"/>
                                <TextBox Style="{StaticResource data}" Text="{Binding ModuleAMonCellNum}"/>
                                <TextBox Style="{StaticResource receiveData}" Text="{Binding ModuleAMonCellNum_rec}"/>
                                <Label Content="子模块A监控温感数目:" VerticalAlignment="Center" Margin="3"  Style="{StaticResource label_gy}"/>
                                <TextBox Style="{StaticResource data}" Text="{Binding ModuleAMonTemperatureNum}"/>
                                <TextBox Style="{StaticResource receiveData}" Text="{Binding ModuleAMonTemperatureNum_rec}"/>
                            </StackPanel>-->
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" Grid.ColumnSpan="2">
                            <StackPanel >
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked17}">
                                            <!--<i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Checked">
                                                    <i:InvokeCommandAction Command="{Binding ClickFacInfo17}" />
                                                </i:EventTrigger
                                            </i:Interaction.Triggers>-->
                                        </CheckBox>
                                        <Label Content="info17" Style="{StaticResource infoNum}"/>

                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource bmunum}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.SlaveNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.SlaveNum_rec}"/>
                                            <Label Content="{DynamicResource junhengmode}" Width="430" VerticalAlignment="Center" Margin="3"  />
                                            <ComboBox FontSize="10" Margin="3" Width="80" VerticalContentAlignment="Center" ItemsSource="{Binding BalanceModeList}" SelectedIndex="{Binding FactoryConfig.SelectBalanceMode}"/>
                                            <!--<TextBox Style="{StaticResource data}" Text="{Binding CellBalanMode}"/>-->
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellBalanMode_rec}"/>
                                            <Label Content="{DynamicResource bmucellcount}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ChildModuleMonCellNumber}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ChildModuleMonCellNumber_rec}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource bmuwgcount}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ChildMonModuleTemperatureNumber}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ChildMonModuleTemperatureNumber_rec}"/>
                                            <Label Content="{DynamicResource zmkAcount1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleAMonCellNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleAMonCellNum_rec}"/>
                                            <Label Content="{DynamicResource zmkAcount2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleAMonTemperatureNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleAMonTemperatureNum_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>


                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked1}"/>
                                        <Label Content="info1" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource cellhigh1}" VerticalAlignment="Center" Margin="3" />
                                            <TextBox  Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolHighAlarmFirst}"/>
                                            <TextBox Text="{Binding FactoryConfig.CellVolHighAlarmFirst_rec,Mode=TwoWay}" Style="{StaticResource receiveData}"/>
                                            <Label Content="{DynamicResource cellhigh2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Text="{Binding FactoryConfig.CellVolHighAlarmSecond}" Style="{StaticResource data}"/>
                                            <TextBox  Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolHighAlarmSecond_rec,Mode=TwoWay}"/>
                                            <Label Content="{DynamicResource cellhigh3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Text="{Binding FactoryConfig.CellVolHighAlarmThird}" Style="{StaticResource data}"/>
                                            <TextBox  Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolHighAlarmThird_rec,Mode=TwoWay}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked2}"/>
                                        <Label Content="info2" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource cellhighrm1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolHighAlarmRemoveFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolHighAlarmRemoveFirst_rec,Mode=TwoWay}"/>
                                            <Label Content="{DynamicResource cellhighrm2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolHighAlarmRemoveSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolHighAlarmRemoveSecond_rec,Mode=TwoWay}"/>
                                            <Label Content="{DynamicResource cellhighrm3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolHighAlarmRemoveThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolHighAlarmRemoveThird_rec,Mode=TwoWay}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>


                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked3}"/>
                                        <Label Content="info3" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource celllow1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolLowAlarmFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolLowAlarmFirst_rec}"/>
                                            <Label Content="{DynamicResource celllow2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolLowAlarmSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolLowAlarmSecond_rec}"/>
                                            <Label Content="{DynamicResource celllow3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolLowAlarmThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolLowAlarmThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>


                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked4}"/>
                                        <Label Content="info4" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource celllowrm1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}"  Text="{Binding FactoryConfig.CellVolLowAlarmRemoveFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolLowAlarmRemoveFirst_rec}"/>
                                            <Label Content="{DynamicResource celllowrm2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolLowAlarmRemoveSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolLowAlarmRemoveSecond_rec}"/>
                                            <Label Content="{DynamicResource celllowrm3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellVolLowAlarmRemoveThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellVolLowAlarmRemoveThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked5}"/>
                                        <Label Content="info5" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource temhigh1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmFirst_rec}"/>
                                            <Label Content="{DynamicResource temhigh2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmSecond_rec}"/>
                                            <Label Content="{DynamicResource temhigh3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmThird_rec}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource temhighrm1}" VerticalAlignment="Center" Margin="3"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmRemoveFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmRemoveFirst_rec}"/>
                                            <Label Content="{DynamicResource temhighrm2}" VerticalAlignment="Center" Margin="3"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmRemoveSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmRemoveSecond_rec}"/>
                                            <Label Content="{DynamicResource temhighrm3}" VerticalAlignment="Center" Margin="3"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmRemoveThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureHighAlarmRemoveThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked6}"/>
                                        <Label Content="info6" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource temlow1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmFirst_rec}"/>
                                            <Label Content="{DynamicResource temlow2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmSecond_rec}"/>
                                            <Label Content="{DynamicResource temlow3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmThird_rec}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource temlowrm1}" VerticalAlignment="Center" Margin="3"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmRemoveFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmRemoveFirst_rec}"/>
                                            <Label Content="{DynamicResource temlowrm2}" VerticalAlignment="Center" Margin="3"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmRemoveSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmRemoveSecond_rec}"/>
                                            <Label Content="{DynamicResource temlowrm3}" VerticalAlignment="Center" Margin="3"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmRemoveThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.CellTemperatureLowAlarmRemoveThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked7}"/>
                                        <Label Content="info7" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhhigh1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmFirst_rec}"/>
                                            <Label Content="{DynamicResource jhhigh2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmSecond_rec}"/>
                                            <Label Content="{DynamicResource jhhigh3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked8}"/>
                                        <Label Content="info8" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhhighrm1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmRemoveFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmRemoveFirst_rec}"/>
                                            <Label Content="{DynamicResource jhhighrm2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmRemoveSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmRemoveSecond_rec}"/>
                                            <Label Content="{DynamicResource jhhighrm3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmRemoveThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentHighAlarmRemoveThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked9}"/>
                                        <Label Content="info9" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhlow1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmFirst_rec}"/>
                                            <Label Content="{DynamicResource jhlow1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmSecond_rec}"/>
                                            <Label Content="{DynamicResource jhlow1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked10}"/>
                                        <Label Content="info10" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhlowrm1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmRemoveFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmRemoveFirst_rec}"/>
                                            <Label Content="{DynamicResource jhlowrm2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmRemoveSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmRemoveSecond_rec}"/>
                                            <Label Content="{DynamicResource jhlowrm3}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmRemoveThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentLowAlarmRemoveThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked11}"/>
                                        <Label Content="info11" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhcur1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentSetValueFirst}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentSetValueFirst_rec}"/>
                                            <Label Content="{DynamicResource jhcur2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentSetValueSecond}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentSetValueSecond_rec}"/>
                                            <Label Content="{DynamicResource jhcur2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanCurrentSetValueThird}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanCurrentSetValueThird_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked12}"/>
                                        <Label Content="info12" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhstartvol}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanVolOpenValue}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanVolOpenValue_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked13}"/>
                                        <Label Content="info13" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhendvol}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanVolCloseValue}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanVolCloseValue_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked14}"/>
                                        <Label Content="info14" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhstartyc}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanVolDifOpenValue}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanVolDifOpenValue_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked15}"/>
                                        <Label Content="info15" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhendyc}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.BalanVolDifCloseValue}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.BalanVolDifCloseValue_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked16}"/>
                                        <Label Content="info16" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource jhstarttem}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}"  Text="{Binding FactoryConfig.CellBalanTemperatureOpenValue}"/>
                                            <TextBox Style="{StaticResource receiveData}"  Text="{Binding FactoryConfig.CellBalanTemperatureOpenValue_rec}"/>
                                            <Label Content="{DynamicResource jhendtem}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}"  Text="{Binding FactoryConfig.CellBalanTemperatureCloseValue}"/>
                                            <TextBox Style="{StaticResource receiveData}"  Text="{Binding FactoryConfig.CellBalanTemperatureCloseValue_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>



                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked18}"/>
                                        <Label Content="info18" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource moduleb1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleBMonCellNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleBMonCellNum_rec}"/>
                                            <Label Content="{DynamicResource moduleb2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleBMonTemperatureNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleBMonTemperatureNum_rec}"/>
                                            <Label Content="{DynamicResource modulec1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleCMonCellNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleCMonCellNum_rec}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource modulec2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleCMonTemperatureNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleCMonTemperatureNum_rec}"/>
                                            <Label Content="{DynamicResource moduled1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleDMonCellNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleDMonCellNum_rec}"/>
                                            <Label Content="{DynamicResource moduled2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleDMonTemperatureNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleDMonTemperatureNum_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked19}"/>
                                        <Label Content="info19" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">

                                            <Label Content="{DynamicResource modulee1}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleEMonCellNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleEMonCellNum_rec}"/>
                                            <Label Content="{DynamicResource modulee2}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.ModuleEMonTemperatureNum}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.ModuleEMonTemperatureNum_rec}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource birthyear}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackProYear}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackProYear_rec}"/>
                                            <Label Content="{DynamicResource birthmonth}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackProMonth}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackProMonth_rec}"/>
                                            <Label Content="{DynamicResource birthday}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackProDay}"/>
                                            <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackProDay_rec}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding FactoryConfig.IsChecked20}"/>
                                        <Label Content="info20" Style="{StaticResource infoNum}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="{DynamicResource num}" VerticalAlignment="Center" Margin="3"  />
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.SerialNum}"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.SerialNum_rec}"/>
                                            <!--<Label Content="电池组项目批量号1:" VerticalAlignment="Center" Margin="3"  />
                                    <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackBatchNumberData1}"/>
                                    <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackBatchNumberData1_rec}"/>
                                    <Label Content="电池组项目批量号2:" VerticalAlignment="Center" Margin="3"  />
                                    <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackBatchNumberData2}"/>
                                    <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackBatchNumberData2_rec}"/>
                                    <Label Content="电池组项目批量号3:" VerticalAlignment="Center" Margin="3"  />
                                    <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackBatchNumberData3}"/>
                                    <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackBatchNumberData3_rec}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                    <Label Content="电池组项目批量号4:" VerticalAlignment="Center" Margin="3"  />
                                    <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackBatchNumberData4}"/>
                                    <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackBatchNumberData4_rec}"/>
                                    <Label Content="电池组项目批量号5:" VerticalAlignment="Center" Margin="3"  />
                                    <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackBatchNumberData5}"/>
                                    <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackBatchNumberData5_rec}"/>
                                    <Label Content="电池组项目批量号6:" VerticalAlignment="Center" Margin="3"  />
                                    <TextBox Style="{StaticResource data}" Text="{Binding FactoryConfig.PackBatchNumberData6}"/>
                                    <TextBox Style="{StaticResource receiveData}" Text="{Binding FactoryConfig.PackBatchNumberData6_rec}"/>-->
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <Border BorderBrush="#5bc0de" BorderThickness="0.4"  Opacity=".3"></Border>

                            </StackPanel>
                        </ScrollViewer>

                    </Grid>






                </TabItem>
                <TabItem Header="{DynamicResource peizhiinfo}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[1]}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35*"/>
                            <ColumnDefinition Width="125*"/>
                            <ColumnDefinition Width="477*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.ColumnSpan="3" >
                            <StackPanel Orientation="Horizontal">
                                <TextBox Width="70" Height="30" Margin="5" Text="{Binding AddBMUConfigNum}"/>
                                <Button Width="90" Height="30" Content="{DynamicResource addbmu}" Margin="5" Command="{Binding AddBMUConfigCommand}" Style="{DynamicResource ButtonStyle1}"/>
                                <Button Width="180" Height="30" Content="{DynamicResource clearconfig}" Margin="5" Command="{Binding ClearBMUConfigCommand}" Style="{DynamicResource ButtonStyle1}"/>
                                <Button Width="170" Height="30" Content="{DynamicResource readconfig}" Margin="15,5,5,5" Command="{Binding ReadConfigCommand}" Style="{DynamicResource ButtonStyle1}"/>
                                <Button Width="170" Height="30" Content="{DynamicResource saveconfig}" Margin="5" Command="{Binding SaveConfigCommand}" Style="{DynamicResource ButtonStyle1}"/>
                                <Button x:Name="sendConfig" Width="170" Height="30" Content="{DynamicResource sendconfig}" Margin="5" Command="{Binding SendConfigCommand}" Style="{DynamicResource ButtonStyle1}"/>
                                <!--<Label Width="120" Height="30" FontSize="10" Content="{Binding Info}" Margin="15,5,5,5" Foreground="Red" VerticalContentAlignment="Center"/>-->
                            </StackPanel>
                        </Grid>
                        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" VerticalAlignment="Center" Margin="0,6,0.333,6">
                            <StackPanel Orientation="Horizontal" >
                                <Label Content="{DynamicResource allselect}" FontSize="12" Margin="3,0,0,0" HorizontalContentAlignment="Left" Width="45"/>
                                <CheckBox Margin="8,0,0,0" >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding All_Check}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="Unchecked">
                                            <i:InvokeCommandAction Command="{Binding All_UnCheck}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>
                            </StackPanel>
                            <Border BorderBrush="#333" BorderThickness=".6"  Opacity=".5"></Border>
                        </StackPanel>
                        <Grid Grid.Row="2" Grid.ColumnSpan="3" Margin="0,0,0.333,-0.333" >
                            <TabControl x:Name="BMUConfigTC" TabStripPlacement="Left" ItemsSource="{Binding BmuConfigList}" SelectedIndex="0">
                            </TabControl>
                        </Grid>
                    </Grid>

                </TabItem>
                <TabItem Header="{DynamicResource bmuinfo}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[2]}" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <StackPanel Margin="5" Grid.ColumnSpan="4">
                            <StackPanel Orientation="Horizontal">
                                <!--<TextBlock Text="最大从机编号：" VerticalAlignment="Center" Foreground="Black"></TextBlock>
                                <TextBox x:Name="tx" Width="80" Height="25" Margin="0,0,10,0" VerticalContentAlignment="Center" Text="{Binding BMUCNT}"  PreviewTextInput="tb_PreviewTextInput" input:InputMethod.IsInputMethodEnabled="False" >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding BmuTextChangedCommand}" CommandParameter="{Binding ElementName=tx}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </TextBox>-->
                                <!--<TextBlock Text="从机数：" VerticalAlignment="Center" Foreground="Black"></TextBlock>
                                <TextBox Width="80" Height="25" Margin="0,0,10,0" VerticalContentAlignment="Center" Text="{Binding BMUCOUNTS}"  PreviewTextInput="tb_PreviewTextInput" input:InputMethod.IsInputMethodEnabled="False" >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding BmuTextChangedCommand}" CommandParameter="{Binding ElementName=tx}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>-->
                            </StackPanel>

                            <StackPanel Margin="0,5,0,0"></StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsCheckAllInfo}" Content="{DynamicResource allselect}" >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding AllInfoCheckCommand}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="Unchecked">
                                            <i:InvokeCommandAction Command="{Binding AllInfoUnCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>
                                <CheckBox IsChecked="{Binding IsSendBmuInfo}" Content="{DynamicResource readall}" Margin="10,0,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuInfoCheckCommand}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="Unchecked">
                                            <i:InvokeCommandAction Command="{Binding BmuInfoUnCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </CheckBox>
                                <CheckBox IsChecked="{Binding IsSendBmuCellInfo}" Margin="10,0,0,0" Content="{DynamicResource readcellvol}" >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuCellInfoCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </CheckBox>
                                <CheckBox IsChecked="{Binding IsSendBmuTempInfo}" Margin="10,0,0,0" Content="{DynamicResource readtem}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuTempInfoCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </CheckBox>
                                <CheckBox IsChecked="{Binding IsSendBmuSLSInfo}" Margin="10,0,0,0" Content="{DynamicResource readvolstates}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuSLSInfoCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                <CheckBox IsChecked="{Binding IsSendBmuSLSTMPInfo}"  Content="{DynamicResource readtemstates}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuSLSTMPInfoCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </CheckBox>
                                <CheckBox Margin="10,0,0,0" IsChecked="{Binding IsSendBmuTMOSInfo}" Content="{DynamicResource readtmostem}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuTMOSInfoCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>
                                <CheckBox Margin="10,0,0,0" IsChecked="{Binding IsSendBalanceInfo}" Content="{DynamicResource readjh}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding BmuBalanceInfoCheckCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>
                                <CheckBox Margin="10,0,0,0" IsChecked="{Binding IsReadVersionInfo}" Content="{DynamicResource readversion}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding ReadVersionCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>

                            </StackPanel>
                            <StackPanel Margin="0,5,0,0"></StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{DynamicResource readbmu}" Style="{DynamicResource ButtonStyle1}" Width="90" Command="{Binding ReadBMUCommand}" Height="25" ></Button>
                                <Button Content="{DynamicResource clearbmu}" Style="{DynamicResource ButtonStyle1}" Width="90" Command="{Binding ClearBMUCommand}" Height="25" Margin="5,0,0,0"></Button>
                                <Button Content="{DynamicResource readconfig}" Style="{DynamicResource ButtonStyle1}" Width="140" Command="{Binding ReadCfgCommand}" Height="25" Margin="5,0,0,0"></Button>
                                <Button Content="{DynamicResource readdiag}" Style="{DynamicResource ButtonStyle1}" Width="120" Command="{Binding ReadDiaCommand}" Height="25" Margin="5,0,0,0"></Button>
                            </StackPanel>
                            <!--<StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                                <Button Content="{DynamicResource readversion}" Style="{DynamicResource ButtonStyle1}" Width="145" Command="{Binding ReadVersion}" Height="25" Margin="0,5,0,0"></Button>
                                <StackPanel x:Name="showversion" Orientation="Horizontal"/>
                            </StackPanel>-->
                        </StackPanel>

                        <ScrollViewer Style="{StaticResource Waiting2Styles}" Grid.Row="1" Margin="0,10,0,0">
                            <Grid x:Name="grid" Grid.Row="1" Grid.ColumnSpan="4" />
                        </ScrollViewer>




                    </Grid>

                </TabItem>

                <TabItem Header="{DynamicResource slavePassvieEqu}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[6]}">
                    <ScrollViewer Style="{StaticResource Waiting2Styles}" HorizontalScrollBarVisibility="Auto">
                        <StackPanel>
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <Label Content="从机编号(十进制)：" Width="140"/>
                                <TextBox Width="50" Text="20"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" x:Name="slavePasEqu"/>
                            <StackPanel HorizontalAlignment="Left">
                                <Button Style="{DynamicResource ButtonStyle1}" Content="发送" Width="60" Margin="5" Height="25" Command="{Binding SendSPECommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="{DynamicResource downbootloader}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[3]}">
                    <Grid>
                        <TabControl x:Name="bootloadtb" TabStripPlacement="Left">
                            <TabItem Header="{DynamicResource master}">
                                <StackPanel>

                                    <Label Content="{DynamicResource master}" HorizontalContentAlignment="Center" FontSize="25"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Width="300" Height="22" Text="{Binding BootLoaderUrl[0]}"/>
                                        <Button Content="{DynamicResource readfile}" Margin="5" Command="{Binding ReadMasterBootLoaderUrlCommand}"/>
                                        <Button Content="{DynamicResource down}" Margin="5" IsEnabled="{Binding Masterbootisenable}" Command="{Binding StartMasterBootLoaderCommand}" CommandParameter="{Binding ElementName=bootloadtb}"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,20,0,0" HorizontalAlignment="Left" Orientation="Vertical">
                                        <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding MaxpbMaster,Mode=TwoWay}" Value="{Binding PBValueMaster,Mode=TwoWay}"/>
                                        <!--<StackPanel Orientation="Horizontal">
                                            <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[0]}"/>
                                            <Label Content="主机" Width="60"/>

                                            <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Height="12" Width="700" Minimum="0" Maximum="{Binding Maxpb[0],Mode=TwoWay}" Value="{Binding PBValue[0],Mode=TwoWay}"/>

                                        </StackPanel>-->
                                    </StackPanel>
                                </StackPanel>
                            </TabItem>
                            <TabItem Header="{DynamicResource num1group}">
                                <StackPanel>

                                    <Label Content="{DynamicResource num1group}" HorizontalContentAlignment="Center" FontSize="25"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Width="300" Height="22" Text="{Binding BootLoaderUrl[1]}"/>
                                        <Button Content="{DynamicResource readfile}" Margin="5" Command="{Binding ReadBootLoaderUrlCommand}"/>
                                        <Button Content="{DynamicResource down}" Margin="5" Command="{Binding StartBootLoaderCommand}" CommandParameter="{Binding ElementName=bootloadtb}"/>
                                    </StackPanel>
                                    <ScrollViewer Style="{StaticResource Waiting2Styles}" HorizontalScrollBarVisibility="Auto">
                                        <Button Content="Button" HorizontalAlignment="Right" Margin="0,0,191.8,50" Width="94" Command="{Binding ReadBMUCommand}" Click="Button_Click"/>
                                    </ScrollViewer>

                                </StackPanel>
                            </TabItem>
                            <TabItem Header="{DynamicResource num2group}">
                                <StackPanel>
                                    <Label Content="{DynamicResource num2group}" HorizontalContentAlignment="Center" FontSize="25"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Width="300" Height="22" Text="{Binding BootLoaderUrl[2]}"/>
                                        <Button Content="{DynamicResource readfile}" Margin="5" Command="{Binding ReadBootLoaderUrlCommand2}"/>
                                        <Button Content="{DynamicResource down}" Margin="5" Command="{Binding StartBootLoaderCommand}" CommandParameter="{Binding ElementName=bootloadtb}"/>
                                    </StackPanel>
                                    <ScrollViewer Style="{StaticResource Waiting2Styles}">
                                        <StackPanel Margin="0,20,0,0" HorizontalAlignment="Left" Orientation="Vertical">
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[16]}"/>
                                                <Label Content="BMU1" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[16],Mode=TwoWay}" Value="{Binding PBValue[16],Mode=TwoWay}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[17]}"/>
                                                <Label Content="BMU2" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[17],Mode=TwoWay}" Value="{Binding PBValue[17],Mode=TwoWay}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[18]}"/>
                                                <Label Content="BMU3" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[18]}" Value="{Binding PBValue[18]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[19]}"/>
                                                <Label Content="BMU4" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[19]}" Value="{Binding PBValue[19]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[20]}"/>
                                                <Label Content="BMU5" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[20]}" Value="{Binding PBValue[20]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[21]}"/>
                                                <Label Content="BMU6" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[21]}" Value="{Binding PBValue[21]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[22]}"/>
                                                <Label Content="BMU7" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[22]}" Value="{Binding PBValue[22]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[23]}"/>
                                                <Label Content="BMU8" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[23]}" Value="{Binding PBValue[23]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[24]}"/>
                                                <Label Content="BMU9" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[24]}" Value="{Binding PBValue[24]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[25]}"/>
                                                <Label Content="BMU10" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[25]}" Value="{Binding PBValue[25]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[26]}"/>
                                                <Label Content="BMU11" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[26]}" Value="{Binding PBValue[26]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[27]}"/>
                                                <Label Content="BMU12" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[27]}" Value="{Binding PBValue[27]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[28]}"/>
                                                <Label Content="BMU13" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[28]}" Value="{Binding PBValue[28]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[29]}"/>
                                                <Label Content="BMU14" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[29]}" Value="{Binding PBValue[29]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[30]}"/>
                                                <Label Content="BMU15" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[30]}" Value="{Binding PBValue[30]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[31]}"/>
                                                <Label Content="BMU16" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[31]}" Value="{Binding PBValue[31]}"/>
                                            </StackPanel>
                                            <!--<StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[32]}"/>
                                                <Label Content="BMU16" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[32]}" Value="{Binding PBValue[32]}"/>
                                            </StackPanel>-->
                                        </StackPanel>
                                    </ScrollViewer>
                                </StackPanel>
                            </TabItem>
                            <TabItem Header="{DynamicResource num3group}">
                                <StackPanel>
                                    <Label Content="{DynamicResource num3group}" HorizontalContentAlignment="Center" FontSize="25"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Width="300" Height="22" Text="{Binding BootLoaderUrl[3]}"/>
                                        <Button Content="{DynamicResource readfile}" Margin="5" Command="{Binding ReadBootLoaderUrlCommand3}"/>
                                        <Button Content="{DynamicResource down}" Margin="5" Command="{Binding StartBootLoaderCommand}" CommandParameter="{Binding ElementName=bootloadtb}"/>
                                    </StackPanel>
                                    <ScrollViewer Style="{StaticResource Waiting2Styles}">
                                        <StackPanel Margin="0,20,0,0" HorizontalAlignment="Left" Orientation="Vertical">
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[32]}"/>
                                                <Label Content="BMU1" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[32],Mode=TwoWay}" Value="{Binding PBValue[32],Mode=TwoWay}"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[33]}"/>
                                                <Label Content="BMU2" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[33],Mode=TwoWay}" Value="{Binding PBValue[33],Mode=TwoWay}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[34]}"/>
                                                <Label Content="BMU3" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[34]}" Value="{Binding PBValue[34]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[35]}"/>
                                                <Label Content="BMU4" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[35]}" Value="{Binding PBValue[35]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[36]}"/>
                                                <Label Content="BMU5" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[36]}" Value="{Binding PBValue[36]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[37]}"/>
                                                <Label Content="BMU6" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[37]}" Value="{Binding PBValue[37]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[38]}"/>
                                                <Label Content="BMU7" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[38]}" Value="{Binding PBValue[38]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[39]}"/>
                                                <Label Content="BMU8" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[39]}" Value="{Binding PBValue[39]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[40]}"/>
                                                <Label Content="BMU9" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[40]}" Value="{Binding PBValue[40]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[41]}"/>
                                                <Label Content="BMU10" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[41]}" Value="{Binding PBValue[41]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[42]}"/>
                                                <Label Content="BMU11" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[42]}" Value="{Binding PBValue[42]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[43]}"/>
                                                <Label Content="BMU12" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[43]}" Value="{Binding PBValue[43]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[44]}"/>
                                                <Label Content="BMU13" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[44]}" Value="{Binding PBValue[44]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[45]}"/>
                                                <Label Content="BMU14" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[45]}" Value="{Binding PBValue[45]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[46]}"/>
                                                <Label Content="BMU15" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[46]}" Value="{Binding PBValue[46]}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[47]}"/>
                                                <Label Content="BMU16" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[47]}" Value="{Binding PBValue[47]}"/>
                                            </StackPanel>
                                            <!--<StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="20,0,0,0" IsChecked="{Binding CBArray[48]}"/>
                                                <Label Content="BMU16" Width="60"/>
                                                <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb[48]}" Value="{Binding PBValue[48]}"/>
                                            </StackPanel>-->
                                        </StackPanel>
                                    </ScrollViewer>
                                </StackPanel>
                            </TabItem>
                            <Button Content="Button" Height="100" Width="75"/>
                        </TabControl>


                        <!--<StackPanel Margin="0,120,0,0" HorizontalAlignment="Center">
                            <ProgressBar HorizontalAlignment="Center" Orientation="Horizontal" Width="700" Height="12" Minimum="0" Maximum="{Binding Maxpb}" Value="{Binding PBValue}"/>
                        </StackPanel>-->
                    </Grid>
                </TabItem>
                <TabItem Header="{DynamicResource S19hebing}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[4]}">
                    <Grid>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Width="300" Height="22" Text="{Binding BootLoaderFileOneUrl}"/>
                                <Button Content="{DynamicResource readfile1}" Margin="5" Command="{Binding ReadBootLoaderFileOneCommand}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Width="300" Height="22" Text="{Binding BootLoaderFileTwoUrl}"/>
                                <Button Content="{DynamicResource readfile2}" Margin="5" Command="{Binding ReadBootLoaderFileTwoCommand}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{DynamicResource merge}" Margin="5" Command="{Binding StartMergeCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </TabItem>
                <TabItem Header="{DynamicResource masterinfo}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[5]}">

                    <ScrollViewer HorizontalScrollBarVisibility="Auto">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                                <Button Content="{DynamicResource handshake}" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding HandShakeWithMaster}"/>
                                <Label Width="130" Content="是否开启数据记录:" Margin="20,4,0,0" HorizontalContentAlignment="Center"/>
                                <CheckBox x:Name="isRecord" Margin="0,4,0,0" IsChecked="{Binding IsOpenMasterRecord}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Checked">
                                            <i:InvokeCommandAction Command="{Binding OpenMasterRecord}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="Unchecked">
                                            <i:InvokeCommandAction Command="{Binding CloseMasterRecord}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </CheckBox>

                                <Button x:Name="shishiBtn" Content="{DynamicResource startshishi}" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterShiShiInfo}" />
                                <TextBox Width="150" Height="22" Text="{Binding DecryptFilePath}" Margin="15,5,5,5"/>
                                <Button Content="{DynamicResource readfile}" Margin="0,5,5,0" Background="#5bc0de" Command="{Binding ReadDecryptFileCommand}"/>
                                <Button Content="{DynamicResource decrypt}" Margin="0,5,5,0" Background="#5bc0de" Command="{Binding DecryptCommand}"/>
                                <Button x:Name="tiedian" Content="{DynamicResource tiedian}" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding Tiedian}" />
                                <Label x:Name="isOnlineBtn" HorizontalContentAlignment="Center" Width="120" Content="{DynamicResource masteroffline}" Margin="50,4,0,0"  Background="#FF6666"/>

                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                                <!--显示版本号-->
                                <!--<TextBox Text="{Binding Version}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[0]}"/>-->
                                <Label Content="{Binding Version}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[0]}"/>
                                <!--<Label Content="{Binding Version}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="Visible"/>-->

                                <!--主机硬件版本号-->
                                <!--<TextBox Text="{Binding MasterHardwareVersion_rec}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[1]}"/>-->
                                <Label Content="{Binding MasterHardwareVersion_rec}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[1]}"/>
                                <!--从机硬件版本号-->
                                <Label Content="{Binding SlaveHardwareVersion_rec}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[2]}"/>
                                <!--<TextBox Text="{Binding SlaveHardwareVersion_rec}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[2]}"/>-->

                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                                <!--显示从机1软件版本号-->
                                <!--<TextBox Text="{Binding SlaveVersionList[1]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[3]}"/>-->
                                <Label Content="{Binding SlaveVersionList[1]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[3]}"/>
                                <!--显示从机2软件版本号-->
                                <!--<TextBox Text="{Binding SlaveVersionList[2]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[4]}"/>-->
                                <Label Content="{Binding SlaveVersionList[2]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[4]}"/>
                                <!--显示从机3软件版本号-->
                                <!--<TextBox Text="{Binding SlaveVersionList[3]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[5]}"/>-->
                                <Label Content="{Binding SlaveVersionList[3]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[5]}"/>
                                <!--显示从机4软件版本号-->
                                <Label Content="{Binding SlaveVersionList[4]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[6]}"/>
                                <!--<TextBox Text="{Binding SlaveVersionList[4]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[6]}"/>-->
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                                <!--显示从机1boot版本号-->
                                <!--<TextBox Text="{Binding SlaveVersionList[5]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[7]}"/>-->
                                <Label Content="{Binding SlaveVersionList[5]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[7]}"/>
                                <!--显示从机2boot版本号-->
                                <Label Content="{Binding SlaveVersionList[6]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[8]}"/>
                                <!--<TextBox Text="{Binding SlaveVersionList[6]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[8]}"/>-->
                                <!--显示从机3boot版本号-->
                                <Label Content="{Binding SlaveVersionList[7]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[9]}"/>
                                <!--<TextBox Text="{Binding SlaveVersionList[7]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[9]}"/>-->
                                <!--显示从机4boot版本号-->
                                <Label Content="{Binding SlaveVersionList[8]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[10]}"/>
                                <!--<TextBox Text="{Binding SlaveVersionList[8]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[10]}"/>-->
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                                <!--显示BMS时钟-->
                                <Label Content="{Binding Clock}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320"/>
                                <!--<TextBox Text="{Binding Clock}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320"/>-->
                                <!--显示VIN-->
                                <Label Content="{Binding VIN_rec}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[12]}"/>
                                <!--<TextBox Text="{Binding VIN_rec}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[12]}"/>-->
                                <!--显示主机boot版本号-->
                                <Label Content="{Binding SlaveVersionList[0]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[13]}"/>
                                <!--<TextBox Text="{Binding SlaveVersionList[0]}" Margin="0,5,5,0" HorizontalContentAlignment="Left" Width="320" Visibility="{Binding VersionVisibility[13]}"/>-->
                            </StackPanel>
                            <StackPanel x:Name="masterChuanShuInfo" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0" Visibility="Collapsed"/>
                            <StackPanel x:Name="masterWenGanInfo" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0" Visibility="Collapsed"/>
                            <!--<StackPanel Orientation="Vertical">
                                <StackPanel HorizontalAlignment="Left">
                                    
                                        <GroupBox Header="{DynamicResource system}" Width="1400" Height="260" Padding="0,5,0,0">
                                        -->
                            <!--<GroupBox Header="{DynamicResource system}" Width="1200" Height="150" Padding="0,5,0,0">-->
                            <!--
                                            <StackPanel HorizontalAlignment="Left">
                                            <StackPanel Orientation="Horizontal" >
                                                <StackPanel HorizontalAlignment="Left">
                                                    <Button Content="{DynamicResource clock}" Width="240" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterClockCommand}"/>
                                                </StackPanel>
                                                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                                    -->
                            <!--电池组剩余容量配置-->
                            <!--
                                                    <Label Content="{DynamicResource capacity}" VerticalContentAlignment="Center" Width="250"/>
                                                    <TextBox Style="{StaticResource data}" Text="{Binding LeftCapacity}" VerticalAlignment="Center"/>
                                                    <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterCapacityCommand}"/>
                                                    -->
                            <!--电池组标称容量配置-->
                            <!--
                                                    <Label Content="{DynamicResource bccapacity}" VerticalContentAlignment="Center" Width="200"/>
                                                    <TextBox Style="{StaticResource data}" Text="{Binding BCCapacity}" VerticalAlignment="Center"/>
                                                    <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterBCCapacityCommand}"/>
                                                    -->
                            <!--电池组总容量配置-->
                            <!--
                                                    <Label Content="{DynamicResource zongcapacity}" VerticalContentAlignment="Center" Width="200"/>
                                                    <TextBox Style="{StaticResource data}" Text="{Binding ZCapacity}" VerticalAlignment="Center"/>
                                                    <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterZCapacityCommand}"/>
                                                </StackPanel>                                                                                           
                                            </StackPanel>
                                            
                                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                                
                                                -->
                            <!--霍尔1电流配置-->
                            <!--
                                                <Label Content="{DynamicResource dlpyconfig}" VerticalContentAlignment="Center" Width="150"/>
                                                <TextBox Style="{StaticResource data}"  Text="{Binding Deviation}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand}"/>
                                                -->
                            <!--霍尔2电流配置-->
                            <!--
                                                <Label Content="{DynamicResource dlpyconfig2}" VerticalContentAlignment="Center" Width="150" Margin="55,0,0,0"/>
                                                <TextBox Style="{StaticResource data}"  Text="{Binding Deviation2}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand2}"/>
                                                -->
                            <!--霍尔3电流配置-->
                            <!--
                                                <Label Content="{DynamicResource dlpyconfig3}" VerticalContentAlignment="Center" Width="150" Margin="50,0,0,0"/>
                                                <TextBox Style="{StaticResource data}"  Text="{Binding Deviation3}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand3}"/>
                                                -->
                            <!--霍尔4电流配置-->
                            <!--
                                                <Label Content="{DynamicResource dlpyconfig4}" VerticalContentAlignment="Center" Width="150" Margin="50,0,0,0"/>
                                                <TextBox Style="{StaticResource data}"  Text="{Binding Deviation4}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand4}"/>                                                
                                            </StackPanel>
                                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                                -->
                            <!--Shunt_1电流偏移配置-->
                            <!--
                                                <Label Content="{DynamicResource shunt_1}" VerticalContentAlignment="Center" Width="150"/>
                                                <TextBox Style="{StaticResource data}" Text="{Binding Shunt_1Cur}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding Shunt1Command}"/>
                                                -->
                            <!--Shunt_2电流偏移配置-->
                            <!--
                                                <Label Content="{DynamicResource shunt_2}" VerticalContentAlignment="Center" Width="150" Margin="55,0,0,0"/>
                                                <TextBox Style="{StaticResource data}" Text="{Binding Shunt_2Cur}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding Shunt2Command}"/>
                                            </StackPanel>
                                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                                -->
                            <!--主机硬件版本号配置-->
                            <!--
                                                <Label Content="{DynamicResource masterhardwareversion}" VerticalContentAlignment="Center" Width="150"/>
                                                <TextBox Style="{StaticResource data}" Width="150" Text="{Binding MasterHardwareVersion}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterHardwareVersionCommand}"/>
                                                -->
                            <!--从机硬件版本号配置-->
                            <!--
                                                <Label Content="{DynamicResource slavehardwareversion}" VerticalContentAlignment="Center" Width="150" Margin="64,0,0,0"/>
                                                <TextBox Style="{StaticResource data}" Width="150" Text="{Binding SlaveHardwareVersion}" VerticalAlignment="Center" />
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding SlaveHardwareVersionCommand}"/>
                                            </StackPanel>
                                            <Border BorderBrush="#5bc0de" BorderThickness="1" Margin="2">
                                                <StackPanel>
                                                    <WrapPanel x:Name="relay_wp" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5,5,0,0" Width="1300" />
                                                    -->
                            <!--<WrapPanel x:Name="relay_wp" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5,5,0,0" Width="1020" />-->
                            <!--
                                                    <StackPanel HorizontalAlignment="Left">
                                                        <Button Content="{DynamicResource jdqconfig}" Width="180" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterRelayCommand}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>
                            </StackPanel>-->
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                                <GroupBox Header="{DynamicResource read}" Padding="0,5,0,0" Width="1600">
                                    <StackPanel>
                                        <StackPanel x:Name="masterShiShiInfo1" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                        <StackPanel>
                                            <TabControl TabStripPlacement="Left"   HorizontalAlignment="Left">

                                                <!--<TabItem Header="总信息" Style="{DynamicResource ModelTabItemStyle}">
                                           
                                        </TabItem>-->
                                                <TabItem Header="{DynamicResource cellinfo}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <StackPanel x:Name="masterShiShiInfo2" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo2_1" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource muxianvol}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <!--info7-->
                                                        <StackPanel x:Name="masterShiShiInfo3" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <!--info8-->
                                                        <StackPanel x:Name="masterShiShiInfo3_1" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource signalstatus}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <StackPanel x:Name="masterShiShiInfo4" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo4_2" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo4_4" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo4_3" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource balancestatus}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <StackPanel x:Name="masterShiShiInfo5" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>

                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource otherinfo}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <StackPanel x:Name="masterShiShiInfo6" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo7" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo7_2" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo8" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        <StackPanel x:Name="masterShiShiInfo9" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource watertem}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <StackPanel x:Name="masterShiShiInfo11" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource faultinfo}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <!--info6-->
                                                        <WrapPanel Width="900" x:Name="masterShiShiInfo10" Orientation="Horizontal"  HorizontalAlignment="Left" Margin="10 10 10 0">

                                                        </WrapPanel>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource nengliang}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <!--info26-31的data[2]-->
                                                        <StackPanel>
                                                            <StackPanel x:Name="masterShiShiInfo12" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </TabItem>
                                                <TabItem Header="{DynamicResource faultlevel}" Style="{DynamicResource ModelTabItemStyle}">
                                                    <StackPanel>
                                                        <StackPanel>
                                                            <StackPanel x:Name="masterShiShiInfo13" Orientation="Vertical"  HorizontalAlignment="Left" Margin="10 10 10 0"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </TabItem>

                                            </TabControl>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>
                            </StackPanel>
                            <!--主机实时信息-->
                        </StackPanel>
                    </ScrollViewer>
                    <!--<TextBox Style="{StaticResource data}" Text="{Binding SlaveNumTarget}" VerticalContentAlignment="Center"/>-->


                </TabItem>
                <TabItem Header="{DynamicResource masterconfig}" Style="{DynamicResource ModelTabItemStyle}" Visibility="{Binding Itemlist[6]}">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto">
                        <StackPanel Orientation="Vertical">
                            <StackPanel HorizontalAlignment="Left">

                                <GroupBox Header="{DynamicResource system}" Width="1400" Height="260" Padding="0,5,0,0">
                                    <!--<GroupBox Header="{DynamicResource system}" Width="1200" Height="150" Padding="0,5,0,0">-->
                                    <StackPanel HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal" >
                                            <StackPanel HorizontalAlignment="Left">
                                                <Button Content="{DynamicResource clock}" Width="240" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterClockCommand}"/>
                                            </StackPanel>
                                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                                <!--电池组剩余容量配置-->
                                                <Label Content="{DynamicResource capacity}" VerticalContentAlignment="Center" Width="250"/>
                                                <TextBox Style="{StaticResource data}" Text="{Binding LeftCapacity}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterCapacityCommand}" />
                                                <!--电池组标称容量配置-->
                                                <Label Content="{DynamicResource bccapacity}" VerticalContentAlignment="Center" Width="200"/>
                                                <TextBox Style="{StaticResource data}" Text="{Binding BCCapacity}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterBCCapacityCommand}"/>
                                                <!--电池组总容量配置-->
                                                <Label Content="{DynamicResource zongcapacity}" VerticalContentAlignment="Center" Width="200"/>
                                                <TextBox Style="{StaticResource data}" Text="{Binding ZCapacity}" VerticalAlignment="Center"/>
                                                <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterZCapacityCommand}"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">

                                            <!--霍尔1电流配置-->
                                            <Label Content="{DynamicResource dlpyconfig}" VerticalContentAlignment="Center" Width="150"/>
                                            <TextBox Style="{StaticResource data}"  Text="{Binding Deviation}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand}"/>
                                            <!--霍尔2电流配置-->
                                            <Label Content="{DynamicResource dlpyconfig2}" VerticalContentAlignment="Center" Width="150" Margin="55,0,0,0"/>
                                            <TextBox Style="{StaticResource data}"  Text="{Binding Deviation2}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand2}"/>
                                            <!--霍尔3电流配置-->
                                            <Label Content="{DynamicResource dlpyconfig3}" VerticalContentAlignment="Center" Width="150" Margin="50,0,0,0"/>
                                            <TextBox Style="{StaticResource data}"  Text="{Binding Deviation3}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand3}"/>
                                            <!--霍尔4电流配置-->
                                            <Label Content="{DynamicResource dlpyconfig4}" VerticalContentAlignment="Center" Width="150" Margin="50,0,0,0"/>
                                            <TextBox Style="{StaticResource data}"  Text="{Binding Deviation4}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterDeviationCommand4}"/>
                                        </StackPanel>
                                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                            <!--Shunt_1电流偏移配置-->
                                            <Label Content="{DynamicResource shunt_1}" VerticalContentAlignment="Center" Width="150"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding Shunt_1Cur}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding Shunt1Command}"/>
                                            <!--Shunt_2电流偏移配置-->
                                            <Label Content="{DynamicResource shunt_2}" VerticalContentAlignment="Center" Width="150" Margin="55,0,0,0"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding Shunt_2Cur}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding Shunt2Command}"/>
                                            <!--放电总能量配置-->
                                            <Label Content="{DynamicResource fangdianznl}" VerticalContentAlignment="Center" Width="150" Margin="55,0,0,0"/>
                                            <TextBox Style="{StaticResource data}" Text="{Binding Fangdznl}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding FangdznlCommand}"/>
                                        </StackPanel>
                                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,5,0">
                                            <!--主机硬件版本号配置-->
                                            <Label Content="{DynamicResource masterhardwareversion}" VerticalContentAlignment="Center" Width="150"/>
                                            <TextBox Style="{StaticResource data}" Width="150" Text="{Binding MasterHardwareVersion}" VerticalAlignment="Center"/>
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterHardwareVersionCommand}"/>
                                            <!--从机硬件版本号配置-->
                                            <Label Content="{DynamicResource slavehardwareversion}" VerticalContentAlignment="Center" Width="150" Margin="64,0,0,0"/>
                                            <TextBox Style="{StaticResource data}" Width="150" Text="{Binding SlaveHardwareVersion}" VerticalAlignment="Center" />
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding SlaveHardwareVersionCommand}"/>
                                            <!--VIN配置-->
                                            <Label Content="{DynamicResource vinconfig}" VerticalContentAlignment="Center" Width="150" Margin="64,0,0,0"/>
                                            <TextBox Style="{StaticResource data}" Width="150" Text="{Binding VIN}" VerticalAlignment="Center" />
                                            <Button Content="{DynamicResource config}" Width="70" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding VINConfigCommand}"/>
                                        </StackPanel>
                                        <Border BorderBrush="#5bc0de" BorderThickness="1" Margin="2">
                                            <StackPanel>
                                                <WrapPanel x:Name="relay_wp" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5,5,0,0" Width="1300" />
                                                <!--<WrapPanel x:Name="relay_wp" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5,5,0,0" Width="1020" />-->
                                                <StackPanel HorizontalAlignment="Left">
                                                    <Button Content="{DynamicResource jdqconfig}" Width="180" Height="30" Background="#5bc0de" Margin="0,5,5,0" Command="{Binding MasterRelayCommand}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </GroupBox>
                                <GroupBox Header="{DynamicResource balancecontrol}" Width="1400" Height="340" Padding="0,5,0,0">
                                    <TabControl x:Name="masterbalancecontrol" TabStripPlacement="Left"   HorizontalAlignment="Left" Visibility="Collapsed">
                                        <!--<TabItem Header="{DynamicResource masterconfig}" Style="{DynamicResource ModelTabItemStyle}">
                                            <StackPanel>
                                                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" VerticalAlignment="Top">
                                                    <Label Content="单体1：" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Width="70" Height="32"/>
                                                    <CheckBox IsChecked="{Binding SlaveBalanceModelList[0].IsBalance[0]}"/>
                                                    <Label Content="单体2：" Width="70" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Height="32"/>
                                                    <CheckBox/>
                                                </StackPanel>
                                                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" VerticalAlignment="Top">
                                                    <Label Content="单体1：" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Width="70" Height="32"/>
                                                    <CheckBox />
                                                    <Label Content="单体2：" Width="70" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Height="32"/>
                                                    <CheckBox/>
                                                </StackPanel>
                                                <StackPanel HorizontalAlignment="Left">
                                                    <Button  Content="均衡" Width="70" Command="{Binding TestCommand}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </TabItem>-->
                                    </TabControl>
                                </GroupBox>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <Button Content="Button" Height="100" Width="75"/>
            </TabControl>
        </Grid>

        <StackPanel Grid.Row="3">

            <Label Width="300" Height="30" FontSize="14" Content="{Binding Info}" Margin="0" Foreground="Red" BorderThickness="1"  VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>
    </Grid>



</mui:ModernWindow>
